import 'package:flutter/material.dart';

class MyArticleScreen extends StatelessWidget{
  const MyArticleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Halaman Artikel'),
      ),
    );
  }
}